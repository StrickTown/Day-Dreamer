//
//  ContentView.swift
//  Day Dreamer
//
//  Created by Adam Strickland on 1/30/24.
//

import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            Image(systemName: "globe")
                .imageScale(.large)
                .foregroundStyle(.tint)
            Text("Hello, world!")
            Text("Test")
            Text("Houston, we're home")
            Text("One last test")
        }
        .padding()
    }
}

#Preview {
    ContentView()
}
